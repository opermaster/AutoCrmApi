namespace AutoCrmApi.Models
{
    public enum UserRole
    {
        Admin = 0,
        Manager = 1,
        Master = 2
    }

    public enum OrderStatus
    {
        Created = 0,
        InProgress = 1,
        Completed = 2,
        Canceled = 3
    }
}
